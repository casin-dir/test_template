#!/usr/bin/env python3

import os

if __name__ == '__main__':
	print('this script running on build')
	stage = os.environ.get('CREATOR_SCRIPT_STAGE')
	print(stage)
	item = os.environ.get('CREATOR_SCRIPT_ITEM')
	print(item)
